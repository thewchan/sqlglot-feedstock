{% set name = "sqlglot" %}
{% set version = "22.3.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sqlglot-{{ version }}.tar.gz
  sha256: ee2985b40601c7b553f17c2c13685f9f831bdfe3edba78fb7b385fc55c5a8a30

build:
  number: 1
  noarch: python

outputs:
  - name: {{ name }}
    build:
      script: python -m pip install . --no-deps --no-build-isolation -vv
      noarch: python

    requirements:
      host:
        - pip
        - python >=3.7
        - setuptools_scm
      run:
        - python >=3.7

    test:
      source_files:
        - tests
      imports:
        - sqlglot
      commands:
        - pip check
        - pytest -vvv --capture=tee-sys tests
      requires:
        - pip
        - openjdk
        - pandas
        - pyspark
        - pytest
        - python-dateutil

  - name: {{ name }}-rs
    build:
      script: echo Nothing to do.
      nochar: python

    requirements:
      host:
        - pip
        - python >=3.7
        - setuptools_scm
      run:
        - python >=3.7
        - {{ pin_subpackage('sqlglot', exact=True) }}
        - sqlglotrs

    test:
      source_files:
        - tests
      imports:
        - sqlglot
      commands:
        - pip check
        - pytest -vvv --capture=tee-sys tests
      requires:
        - pip
        - openjdk
        - pandas
        - pyspark
        - pytest
        - python-dateutil
        - python-duckdb

about:
  home: https://github.com/tobymao/sqlglot
  summary: An easily customizable SQL parser and transpiler
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - tobymao
    - thewchan
